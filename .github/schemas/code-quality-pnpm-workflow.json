{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Workflow calling code-quality-pnpm-workflow reusable workflow",
  "allOf": [
    { "$ref": "https://json.schemastore.org/github-workflow.json" },
    {
      "properties": {
        "jobs": {
          "type": "object",
          "properties": {
            "code-quality": {
              "type": "object",
              "properties": {
                "uses": {
                  "type": "string",
                  "pattern": ".*/code-quality-pnpm-workflow.yaml@.*"
                },
                "with": {
                  "type": "object",
                  "properties": {
                    "check-command": {
                      "type": "string",
                      "description": "Required. Command to run code quality and build checks."
                    },
                    "node-version": {
                      "type": "string",
                      "description": "Optional. Node.js version. Uses the workflow's default if not provided."
                    },
                    "setup-command": {
                      "type": "string",
                      "description": "Optional. Shell command to run after dependency installation and bootstrap, before the check command. Use for installing system dependencies. The consumer is responsible for sudo, package-manager flags, and any necessary index updates (e.g., apt-get update)."
                    }
                  },
                  "required": ["check-command"],
                  "additionalProperties": false
                }
              },
              "required": ["uses", "with"]
            }
          }
        }
      }
    }
  ]
}
